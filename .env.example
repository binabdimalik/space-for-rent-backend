# Backend Environment Variables Template
# Copy this file to .env and fill in your values
# NEVER commit .env with real secrets to git!

# Flask Configuration
FLASK_APP=app.py
FLASK_ENV=development
# Change this to 'production' when deploying

# Secret Key - CHANGE THIS IN PRODUCTION!
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-super-secret-key-change-in-production

# Database Configuration
# Development (SQLite)
DATABASE_URL=sqlite:///app.db

# Production (PostgreSQL) - uncomment and configure for production
# DATABASE_URL=postgresql://username:password@localhost:5432/spaceforrent

# Server Configuration
PORT=5555
HOST=0.0.0.0

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,https://rentalhub.dpdns.org

# JWT Configuration (for authentication)
JWT_SECRET_KEY=your-jwt-secret-key-change-in-production
JWT_ACCESS_TOKEN_EXPIRES=86400
